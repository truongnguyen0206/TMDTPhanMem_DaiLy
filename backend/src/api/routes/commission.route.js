const express = require('express');
const router = express.Router();

// Placeholder routes - cần implement sau
router.get('/test', (req, res) => {
  res.json({ message: 'Commission routes working' });
});

module.exports = router;
